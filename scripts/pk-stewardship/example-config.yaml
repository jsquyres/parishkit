# Development metadata only; no real credentials or configured parish.
deployment:
  schema_version: 1
  profile: development
  service_role: web
  public_origin: http://localhost:8000
  trusted_proxy_hops: 0
  postgres:
    host: postgres
    port: 5432
    name: stewardship
    user: stewardship
  valkey:
    host: valkey
    port: 6379
    database: 0
